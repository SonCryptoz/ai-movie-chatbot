import { Metadata } from "next";
import SettingsPage from "./settings";

export const metadata: Metadata = {
    title: "Settings",
    description: "Configure your AI chatbot preferences",
    icons: {
        icon: "/logo.ico",
    },
};

export default SettingsPage;
